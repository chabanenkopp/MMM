import React from 'react'

const {
  Provider: CheckUserServiceProvider,
  Consumer: CheckUserServiceConsumer,
} = React.createContext()

export { CheckUserServiceProvider, CheckUserServiceConsumer }
